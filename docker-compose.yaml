services:
  frontend:
    build: .
    container_name: ciccte-frontend
    command: bun run dev --host
    ports:
      - "5173:5173"
    networks:
      - db

networks:
  db:
    name: db-net
    external: true
